import java.util.Scanner;
import java.io.FileInputStream;
import java.io.IOException;

public class LetterDataProcessor {
   public static void main(String[] args) throws IOException {
      Scanner scnr = new Scanner(System.in);
      String dataFileName;
      char letterGrade;
      FileInputStream inStream = null;
      Scanner inFS = null;

      dataFileName = scnr.next();

      inStream = new FileInputStream(dataFileName);
      inFS = new Scanner(inStream);

      letterGrade = inFS.next().charAt(0);
      System.out.println(letterGrade);

      inStream.close();
   }
}

// String dataFileName is assigned a file's name read from input. Perform the following tasks:
// Assign inStream with a FileInputStream that opens the file dataFileName for reading.
// Assign inFS with a Scanner created using inStream.